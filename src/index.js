import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <div>
     <h1>Hello World</h1>
     <p>This is a paragraph</p>
  </div>,
  document.getElementById('root')
);
//render will take only single html element to render in first argument
//2nd argument specify where to render